{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",

  "// rewrites": "Routing happens in the browser, so a hard load of /organizations or a refresh anywhere but / must still be served index.html. Files that actually exist in dist are matched before this runs, so the hashed bundles are unaffected.",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],

  "// headers": "Bundle filenames carry a content hash, so a given file never changes and can be cached for a year. index.html itself is deliberately not cached here, or a deploy would not reach anyone already holding it.",
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
